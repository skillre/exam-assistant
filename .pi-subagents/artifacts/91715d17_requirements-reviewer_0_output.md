Now I have all the data needed. Let me compile the findings.

| # | severity | dimension | requirement-ref | finding | recommendation |
|---|----------|-----------|-----------------|---------|----------------|
| 1 | blocker | testability | 目标②+目标验收 | 目标验收缺少目标②（续做恢复已答判分态）和目标④（答疑历史过滤首条注入）的可观察验收项。目标验收只列了 typecheck/test/smoke-e2e/远程实测，没有命令能证明"续做时已答题目显示判分状态"或"历史回读不含系统提示词"。 | 为②补：续做后调 `api.getPractice` 并断言 `gradedById` 非空；为④补：调 `/api/tutor/history` 并断言首条不含系统提示词关键词。 |
| 2 | blocker | testability | 回归验收 | 目标验收有 4 项（typecheck/test/smoke-e2e/远程），回归验收只有 3 项（typecheck/test/smoke-e2e）加一句模糊的"远程回归"。缺少：① SSE 超时触发后 UI 复位的回归验证；② sessionHistory 过滤改动不破坏 `LearningDataCollector.collectTutorHighlights`（后者依赖 `readSessionHistory` 且自行 `.slice(1)`）。 | 回归验收增加：a) `LearningDataCollector` 现有测试（`LearningDataCollector.test.ts`）全绿；b) 远程回归列出具体操作步骤而非"行为一致"。 |
| 3 | blocker | testability | 目标① | SSE 加 AbortController/超时未指定超时时长。"超时"是可配置参数还是固定值？超时后 `streamSse` 是 reject 还是调 `onError`？未定义则无法写验收。 | 在改动点或验收中写明超时值（如 30s）和超时行为（调 `onError` 并返回）。 |
| 4 | blocker | completeness | 目标③ | SettingsPage 当前 `refresh()` 只调 `listProviders()`+`listModels()`，不调任何读回激活模型的接口，`active` 状态始终为空字符串。改动点列了"新增 GET 当前激活模型接口"但 client.ts 无对应方法、SettingsPage 改动点未写清要调用该接口。缺一步"SettingsPage.refresh() 中调 getActiveModel 并 setActive"。 | 改动点 `SettingsPage.tsx` 明确写：refresh() 中增加 `api.getActiveModel()` 调用并 `setActive`；`client.ts` 增加 `getActiveModel` 方法。 |
| 5 | blocker | completeness | 目标② | `finish()`（QuizPage.tsx:135-141）只调 `api.getScorecard`，不调 `localStorage.removeItem(LS_KEY)`。如果 `getScorecard` 抛错，localStorage 残留且用户刷新后仍续做已完结会话。`reset()` 才清 localStorage，但 `finish()` 成功路径也不清——用户看完成绩单需手动点"退出练习"才清。 | `finish()` 的 `onDone` 路径（scorecard 设置成功后）增加 `localStorage.removeItem(LS_KEY)`。 |
| 6 | blocker | completeness | 目标② | `resumeSession`（QuizPage.tsx:64）执行 `setGradedById({})`，续做时已答判分态全部丢失。目标要求"续做恢复已答判分态"，但当前无服务端 API 支持按 sessionId 查已答判分结果。仅改前端无法实现。 | 需明确：a) 新增服务端 `/api/practice/:id/graded` 返回 `Record<questionId, GradeResponse>`，或 b) 从 scope 降级——接受续做不恢复判分态，从目标中删除该子项。 |
| 7 | concern | completeness | 改动点-sessionHistory | `readSessionHistory` 当前不过滤首条注入。改动说"过滤首条注入（与 LearningDataCollector.slice(1) 对齐）"，但 LearningDataCollector（ts:92）做的是 `turns.filter(t => t.role === 'user').slice(1)`——只对 user 轮切片。若 `readSessionHistory` 改为全局跳过首条 entry，则 LearningDataCollector 拿到的 user 轮已少一条，再 `.slice(1)` 会多跳一条。两处逻辑必须对齐。 | 在改动点明确：过滤策略是"跳过首条 user 轮"还是"跳过首条 entry（任何 role）"，并说明 LearningDataCollector 的 `.slice(1)` 是否同步移除。 |
| 8 | concern | completeness | 改动点-tutor/insights | `tutor.ts` 的 `explain`（:32-51）和 `ask`（:58-77）当前 `session.dispose()` 在 try 块末尾，catch 只调 `sse.error()` 不 dispose——这是已知缺陷。改 finally 正确。但 `insights.ts`（:23-35）同样结构，改动点已列。需确认 `openSse` 返回的 `sse` 对象在 `reply.raw` 已关闭后调 `sse.error()` 是否安全（Fastify reply 已发送后写入会抛错）。 | 改动点补充：finally 中先判断 `reply.sent` 再调 `sse.error()`/`sse.done()`，或 catch 中 dispose 顺序先于 sse.error。 |
| 9 | concern | testability | 目标① | SSE 超时/异常后"复位 UI 状态并提示"——TutorPanel 的 `onError` 已设置 `setStreaming(false)` + `setError(msg)`（:50-53），InsightsPage 同理（:38-41）。复位逻辑已有，但无验收证明超时路径（非服务端 500，而是客户端断线/超时）能触发 onError。 | 验收增加：模拟 SSE 断线（如 AbortController.abort()）后断言 UI 不卡"思考中…"。 |
| 10 | concern | feasibility | 目标② | "续做恢复已答判分态"需要知道每题的 attemptId 和判分结果。当前 PracticeSession 结构（types.ts）不含 graded 信息，practiceRepo 无此查询。实现此目标需新增 API+仓储，超出"5 个功能可用性缺陷"的 M 级预估。 | 在改动点补列新增的 API 和仓储，或降级目标②为"续做不丢失进度，判分态从零开始（可接受）"。 |
| 11 | concern | scope | 远程回归验收 | 远程回归写"练习续做、错题本、导入、判分与改造前行为一致"——4 个场景无具体操作步骤、无预期结果。这不是验收，是愿望。 | 每个场景写 1 条具体操作+预期（如"续做：刷新页面→自动恢复到上次进度→显示第 N 题"）。 |
| 12 | suggestion | completeness | 改动点-shared/types.ts | 改动点列"读回接口响应类型（如需）"。GET active model 返回 `ActiveModelSelection`（已存在于 types.ts:162），无需新增类型。但若返回附带 model name（前端需要显示名称），则需新类型。 | 确认 GET 返回体是 `ActiveModelSelection` 还是带 name 的扩展类型，删除"如需"的不确定性。 |
| 13 | suggestion | scope | 整体 | 范围红线写"不引入新依赖"，但改动点涉及 `AbortController`（全局对象，非依赖）和可能的超时库。确认超时用 `AbortSignal.timeout()`（ES2025 原生）而非 `setTimeout` + 外部库。 | 改动点明确用 `AbortSignal.timeout(ms)` 原生方案。 |
| 14 | concern | completeness | 整体 | 5 个目标验收+回归验收均无 sessionHistory 过滤的专项测试。文档说"新增：sessionHistory 过滤用例"但未写在目标验收清单里，只在括号提及。 | 目标验收显式增加一行：`sessionHistory.test.ts` 过滤用例通过。 |