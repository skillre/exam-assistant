# Goal

**Status**: active
**Policy**: list
**Auto-continue**: on
**File**: `20260812142234-w1pmek.md`

## Objective

> 第五批：主观题 essay 题型 + AI 评分 + 批改意见（specs/CHANGES.md 2026-08-12 第五批 v2 已确认，M~L 级）。Done when: ① `pnpm typecheck` 0 错误；② `pnpm test` 全量通过——旧 93 用例全绿 + 新增：questionSchema essay 校验（合法 string 通过、空串/纯空白拒绝）、parseFile essay 解析、csv 导入导出 essay 行往返（含逗号/引号/换行转义正确，answer 列=参考文本）、essay grade 分流单测（mock AiService 对/错各一 + feedback 字段）、评分输出非法（非 JSON/isCorrect 非布尔/缺 feedback）→5xx 且 attempts 不落库、runOnce 超时→5xx 不落库、空/空白答案→400 且不调 AI；③ routes.smoke.test 新增 essay 链路：导入 essay 题成功（旧 CSV 模板行仍可导入）→ grade 返回 isCorrect+feedback → attempts 落库 → wrong.csv 含 essay 行且 answer 列=参考文本、含逗号/引号的用户答案 RFC4180 正确引用；④ 本地 curl 验证（证据落盘 specs/verification/）：essay 导入→grade 返回 isCorrect+feedback、attempts 落库、wrong.csv essay 行转义正确；⑤ `pnpm --filter @exam/server smoke` 51 断言全绿不降；⑥ `git diff` 确认：schema.ts 仅 questions.type CHECK 一行变化、零新依赖、docker/nginx/compose 零改动、v3 教练 prompt（diagnosis/plan/tutor）零改动；⑦ 回归：客观题三题型 grade 毫秒级同步契约不变（grading/grade.ts 规则逻辑零改动仅注释、grade 端点客观题分支零改动、旧 93 用例全绿）、客观题 CSV 导入导出格式不变（旧模板仍可导入）、InsightsPage/HistoryPage/错题本/掌握度零改动（仅数据流经 essay 的 is_correct）、粘贴导入三题型解析与现状一致；⑧ 远程部署后抽查（证据落盘 specs/verification/）：docker compose 重建 healthy、essay 题真实 AI 评分返回 isCorrect+feedback（对/错各一）、远程 wrong.csv 含 essay 行。契约验收全部过独立 auditor。
