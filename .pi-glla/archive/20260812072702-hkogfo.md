# Goal

**Status**: complete
**Policy**: goal
**Auto-continue**: on
**File**: `20260812072702-hkogfo.md`
**Stop reason**: auditor opencode-go-responses/deepseek-v4-flash approved

## Objective

> 第三批：P2 体验优化 + 代码卫生（specs/CHANGES.md 2026-08-10 第三批节已确认，M 级）。体验 5 项：① 轻量 hash 路由——原生 location.hash（#/quiz 等 5 tab），单一 navigate(tab) 收口三处下钻/switchTab（含 filter 重置）/hashchange，非法 hash 回退刷题页，刷新停留当前页，不引入依赖；② 无障碍——QuestionCard 单选 role=radio+方向键/多选 role=checkbox+Tab空格/判断三分支键盘可操作，与现有点击判分并存；③ 错误文案——client req() 失败先读 j.message 再 j.error（400 显示 message 而非裸 Bad Request）；④ selectModel 先 await setActiveModel 成功后再 setActive（失败不回滚假选）；⑤ ProviderForm alert→内联错误 + apiType 枚举（openai-completions/anthropic-messages）+ 编辑模式现值兜底 option。卫生 6 项：⑥ 死代码清扫——删 shared-check.ts、client validateDrafts+共享类型、attemptRepo.listWrongQuestions、wrongBookRepo.getState、无消费方 POST /api/import/validate 端点；providerRepo.get→私有 getRow 内联；⑦ 删 nanoid 死依赖（lockfile 同步）；⑧ 题型映射 6 处 UI 统一 QUESTION_TYPE_LABEL（prompt 2 处不动）+ flash() 抽 util + 删除题库 confirm 统一；⑨ AiService 新增 createInMemoryTutorSession（不查 sessionFile），insights analyze 改用之消孤儿 JSONL；⑩ tutor 路由 in-flight Map<questionId,Promise> 去重防并发双建（不动 schema）；⑪ 测试补缺——parseFile.test（JSON/CSV/xlsx+畸形容错）+ routes.smoke.test（自建 Fastify 按 register*Routes 组装+AiService 桩，DATA_DIR 临时目录，app.inject HTTP 全链路+provider 密文）+ server smoke script。边界：安全项/备份方案不碰；部署架构/nginx/compose 不变；既有表零改动（不重建）；不引入新依赖（nanoid 为删除项）；AI prompt/行为不变。

## Verification contract

① `pnpm typecheck` 0 错误；② `pnpm test` 全量通过——现有 58 用例全绿 + 新增 parseFile 用例（JSON/CSV/xlsx 各解析正确 + 畸形输入不抛）+ routes.smoke 用例（HTTP 链路建库→导入→练习→判分→错题→graded + provider 配 Key 密文入库+列显脱敏）；③ `pnpm --filter @exam/server smoke`（新 script）51 断言全绿；④ `git diff` 确认无新增依赖（nanoid 为删除行）、packages/server/src/db/schema.ts 零改动、docker/nginx/compose 零改动；⑤ 浏览器自动化实测（kimi-webbridge，证据落盘 specs/verification/）：打开 8080 首页默认刷题页、URL 改 #/wrong 直开错题本、刷新停留错题本、非法 hash（#/bogus）回退刷题页、错题本筛选后切 tab 再回 filter 为空、单选/多选/判断选项区键盘可操作并正常判分、触发 400 时页面显示 message 字段文案；⑥ 回归：Tab 切换/下钻链（drillToQuiz/drillToWrong/startTaskQuiz 含一键开练）/错题本 applyFilter/学情四面板与第二批行为一致；⑦ 代码核查：App.tsx 单一 navigate 收口、QuestionCard 三分支键盘、AiService.createInMemoryTutorSession 存在且 insights 使用、tutor 路由 in-flight Map 去重、shared 无 QUESTION_TYPE_LABEL 之外的重复题型映射、/api/import/validate 与 nanoid 与 shared-check.ts 已删。

## Audit history

- 2026-08-12T09:53:32.657Z — disapproved — `opencode-go-responses/deepseek-v4-flash`
- 2026-08-12T11:20:32.940Z — approved — `opencode-go-responses/deepseek-v4-flash`
