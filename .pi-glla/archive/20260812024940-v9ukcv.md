# Goal

**Status**: complete
**Policy**: goal
**Auto-continue**: on
**File**: `20260812024940-v9ukcv.md`
**Stop reason**: auditor opencode-go-responses/deepseek-v4-flash approved

## Objective

> 第一批 P0 功能可用性修复（specs/CHANGES.md 2026-08-10 第一批节已确认）：① SSE 流式调用异常/断线不卡死 UI——sse.ts 加首字节 30s 超时（原生 AbortSignal.timeout，首个 SSE 帧到达后取消超时、流不设总时长）+ onError 回调，TutorPanel/InsightsPage await/catch + finally 复位 streaming/running 状态；② 练习会话 finish() 的 scorecard 成功路径清理 localStorage + 续做恢复已答判分态——新增 GET /api/practice/:id/graded 只读接口（复用 attemptRepo.latestByQuestion，不加表），QuizPage 挂载时调 api.getPracticeGraded 填充 gradedById；③ 设置页读回当前激活模型——client.ts 加 getActiveModel()，SettingsPage.refresh() 调它并 setActive，服务端 routes/models.ts 加 GET /api/models/active 返回 ActiveModelSelection；④ 答疑历史过滤首条注入——readSessionHistory 加可选参数 skipFirstUserTurn=false，tutor history 端点传 true，LearningDataCollector 默认行为不变；⑤ AI 流式错误路径 session.dispose() 移入 finally（幂等），catch 中先 dispose 再 sse.error。边界：安全项（无鉴权/SSRF/依赖漏洞）、备份方案、部署架构、DB schema 均不动；不引入新依赖。

## Verification contract

① `pnpm typecheck` 输出 0 错误；② `pnpm test` 全量通过——现有 32 用例 + 新增 sessionHistory.test.ts（skipFirstUserTurn=true 跳首条、默认 false 行为不变）+ LearningDataCollector.test.ts 4 用例保持全绿；③ `cd packages/server && npx tsx smoke-e2e.ts`（DATA_DIR 指向临时目录）全部断言通过；④ 建练习会话答 2 题后 `curl GET /api/practice/:id/graded` 返回含 2 条 {questionId:{isCorrect,attemptId,answeredAt}}；⑤ 有答疑会话时 `curl GET /api/tutor/history` 首条 user 轮不含系统提示词/正确答案关键词；⑥ 远程 106.37.96.58:6422 部署实测（ssh 执行）：设置页刷新显示当前模型、SSE 讲解流式正常、断连/杀进程后按钮恢复可点不卡"思考中…"、练习续做/错题本/导入/判分 4 场景与改造前一致；⑦ `git diff` 确认 package.json 依赖零新增、packages/server/src/db/schema.ts 与 docker/ 零改动。

## Audit history

- 2026-08-12T03:25:00.005Z — disapproved — `xiaomi-token-plan-cn/mimo-v2.5-pro`
- 2026-08-12T03:32:03.736Z — approved — `opencode-go-responses/deepseek-v4-flash`
