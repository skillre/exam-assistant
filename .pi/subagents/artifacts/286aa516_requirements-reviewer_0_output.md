# 第五批评审（2026-08-12 第五批：主观题 + AI 评分）

| # | severity | dimension | requirement-ref | finding | recommendation |
|---|----------|-----------|-----------------|---------|----------------|
| 1 | blocker | consistency | 目标验收「git diff 确认：schema.ts 零改动」+ 范围红线 | `db/schema.ts:15` 有 `CHECK(type IN ('single','multiple','boolean'))`，SQLite 不能 ALTER CHECK 约束、必须重建表；不改 schema.ts 则 essay 题 INSERT 直接被 CHECK 拒绝，任何 essay 导入必失败——红线与"新增 essay 题型"自相矛盾 | 红线改为"仅允许 questions.type CHECK 扩增 'essay'（其余表/列零改动）"，验收增加"导入 essay 成功 + git diff 仅 CHECK 一行变化" |
| 2 | blocker | completeness | 改动点-服务端 / 影响面 | 导入全路径都过 `import/questionSchema.ts` 校验（parseFile.ts:91/96、parseWithAi.ts:40 均调 validateDrafts），但该文件 `:17` `z.enum(['single','multiple','boolean'])` 与 `:20` `answer: z.union([z.number(), z.array(z.number()), z.boolean()])` 无 string 分支；改动点只列 parseFile.ts，漏了 questionSchema.ts——CSV/JSON/粘贴导入 essay 行在 zod 层即被拒绝，与 #1 是两道独立闸门 | 改动点补 questionSchema.ts（enum 加 'essay'、answer union 加 `z.string()`），影响面与测试用例同步补齐 |
| 3 | concern | feasibility | 改动点「routes/quiz.ts grade 端点 essay 分流：AiService.runOnce 评分（设超时）」 | `AiService.ts:109` `runOnce(systemPrompt, userPrompt)` 签名无 timeout/AbortSignal 参数，改动点未列 AiService 修改；无超时则 LLM 挂起时 grade 请求无限挂、前端 loading 卡死，"失败→5xx→可重试"降级路径永不触发 | 改动点明确 runOnce 增加可选超时参数（或 Agent SDK 超时），routes.smoke.test 补超时→5xx→attempts 不落库用例 |
| 4 | concern | consistency | 目标「全链路打通（导入→…）」+ 范围红线「AI prompt 仅新增 essay 评分 prompt」 | 粘贴导入走 `import/parseWithAi.ts:12`，其 prompt 明文限定 `type 取值：single \| multiple \| boolean`；改动点/影响面完全未提 parseWithAi.ts——若红线禁止改它，粘贴导入 essay 必解析失败或产出错误类型，"导入"链路只通 CSV/JSON | 明确粘贴导入是否支持 essay：支持则改动点补 parseWithAi prompt+用例；不支持则在目标验收与模板文档中声明 essay 仅 CSV/JSON 导入 |
| 5 | concern | completeness | 改动点「复用 planPrompts 的 extractJson 模式」+ 用户三问1 | AI 评分输出 {isCorrect, feedback} 的校验/容错未定义：非 JSON、isCorrect 非 boolean、feedback 缺失、多余键时走抛 5xx 还是重试？且事实核查：planPrompts.ts 内无 extractJson（实际在 `routes/coach.ts:26`），"复用 planPrompts 的 extractJson"位置引用错误 | 参照 diagnosisSchema 模式为评分输出定义 zod 校验，单测补非法输出（非 JSON/isCorrect 非布尔/缺 feedback）用例；文档改为引用 coach.ts:26 或抽公共 extractJson |
| 6 | concern | consistency | 用户三问3「answer 列填参考答案文本」+ 导出「answer=参考文本」 | `schema.ts:18` answer 列语义为 JSON（`-- JSON: number \| number[] \| boolean`），flatRowToRaw 按 type 还原 answer 形态；essay answer 存 JSON.stringify("文本") 还是裸文本未定义，直接影响 wrong.csv 导出 answer 列与 GradeResponse.correctAnswer 展示的往返一致性 | 明确定义 essay answer 存 JSON 编码字符串（与列语义一致），导入/导出补 essay 往返（round-trip）用例 |
| 7 | concern | completeness | 改动点「可提交空答案判定无效」 | 空答案校验只定义在前端 UI；grade 端点绕过 UI 收到空串/纯空白字符串（trim 后空）时行为未定义——是否 400 不调 AI、不落库？ | 后端 grade 端点定义 essay 空/空白答案校验（直接 400，不消耗模型），补单测 |
| 8 | concern | testability | 目标验收「本地 curl：…wrong.csv 含 essay 行」 | wrong.csv 的 essay 行含 wrong_answer（用户作答 JSON）与 answer（参考文本），两列对 essay 都是自由文本，curl 只断言"含 essay 行"无法区分转义/引号正确性（第四批已证明转义是实际 bug 区） | 断言具体化：essay 行 answer 列与导入参考文本一致、含逗号/引号的用户答案被 RFC4180 正确引用 |
| 9 | concern | completeness | 回归验收（显式负面结论） | 回归验收覆盖客观题判分/导入导出/学情闭环/路由/CSV/InsightsPage 零改动，未发现漏项；唯一回归缺口即 #1：schema CHECK 改动与自有红线冲突，属本批必改而非回归风险 | 无新增修复；随 #1 修改后把 CHECK 变更确认并入回归验收的 git diff 步骤 |

**BLOCKERS:**
- #1 目标验收要求 schema.ts 零改动，但 `db/schema.ts:15` 的 type CHECK 不扩 'essay' 则 essay 题无法入库（SQLite CHECK 不可 ALTER，必须改 DDL）——红线与需求自相矛盾，需先定稿 schema 修改边界。
- #2 改动点遗漏 `import/questionSchema.ts`（`z.enum` 三题型 + answer union 无 string），它是导入全路径（CSV/JSON/粘贴）的必经校验闸门，不改则 essay 导入在 zod 层失败——与 #1 是两道独立闸门，均需在确认前补入改动点。