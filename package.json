{
  "name": "exam-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter @exam/dsh-exam run build",
    "typecheck": "pnpm --filter @exam/dsh-exam run typecheck",
    "test": "pnpm --filter @exam/dsh-exam run test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
