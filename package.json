{
  "name": "exam-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm --filter @skillre/dsh-exam run build",
    "typecheck": "pnpm --filter @skillre/dsh-exam run typecheck",
    "test": "pnpm --filter @skillre/dsh-exam run test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.7.0"
  }
}
